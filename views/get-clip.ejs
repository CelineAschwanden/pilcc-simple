<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script>
    //Set base href to root, in case the selected route includes slashes
    var base = document.createElement('base');
    base.href = window.location.protocol + '//' + window.location.hostname + (window.location.port ? ':' + window.location.port : '');
    document.getElementsByTagName('head')[0].appendChild(base);
  </script>
  <link href="style.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/gh/davidshimjs/qrcodejs/qrcode.min.js"></script>
  <title>Pilcc</title>
</head>
<body class="text-white bg-slate-900 h-full font-mono p-6">
  <div id="clip-form" class="h-full">
    <!-- Form buttons -->
    <div class="flex flex-col items-center mt-8">
        <h2 class="text-xl text-white">Here is your stuff, have a nice day.</h2>
        <div class="flex items-center mt-8 lg:mr-10">
            <button onclick="copyContent()" class="bg-slate-700 hover:bg-slate-600 relative focus:outline-none focus:ring-2 focus:ring-slate-400 focus:ring-offset-2 text-sm lg:text-base 
            text-white font-semibold px-8 py-2 rounded-lg flex items-center pointer-events-auto">
            Copy content
            </button>
            <p id="copiedMessage" class="text-sm text-green-500 ml-8 hidden">Copied!</p>
        </div>
    </div>
    <!-- Textarea -->
    <div class="flex flex-col items-center mx-2 lg:mx-8 2xl:mx-10 h-5/6 mt-9" id="home-content">
      <textarea id="clip-content" class="w-full bg-slate-700 text-white p-5 h-4/5 rounded-md"><%= clipContent %></textarea>
    </div>
  </div>
  <script src="get-clip.js"></script>
</body>
</html>