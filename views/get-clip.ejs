<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script>
    //Set base href to root, in case the selected route includes slashes
    var base = document.createElement('base');
    base.href = window.location.protocol + '//' + window.location.hostname + (window.location.port ? ':' + window.location.port : '');
    document.getElementsByTagName('head')[0].appendChild(base);
  </script>
  <link href="style.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/gh/davidshimjs/qrcodejs/qrcode.min.js"></script>
  <title>Pilcc</title>
</head>
<body class="text-white bg-slate-900 font-mono p-6 h-screen">
  <div id="clip-form" class="h-full flex flex-col items-center">
    <!-- Form buttons -->
    <div class="flex flex-col items-center mt-5 h-1/6">
        <h2 class="text-xl text-white">Here is your stuff, have a nice day.</h2>
        <div class="flex items-center space-x-5 mt-8 lg:mr-10">
            <button onclick="copyContent()" type="button" class="px-6 py-2 rounded-md text-sm lg:text-base bg-slate-700 hover:bg-slate-600 
            focus:outline-none focus:ring-2 focus:ring-slate-400 focus:ring-offset-2 text-white font-semibold pointer-events-auto">
            Copy content
            </button>
            <p id="copiedMessage" class="text-sm text-green-500 hidden">Copied!</p>
        </div>
    </div>
    <!-- Textarea -->
    <textarea id="clip-content" class="w-full h-full my-9 md:w-4/5 bg-slate-700 text-white p-5 rounded-md" readonly><%= clipContent %></textarea>
  </div>
  <script src="get-clip.js"></script>
</body>
</html>